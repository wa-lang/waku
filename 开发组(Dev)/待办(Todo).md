## dev@wa-lang.org

当前待办事项，**本贴无需回复可任意编辑**。

- 编译器单元测试
- 支持 build-tag
- 支持 WASI 和 unknown 宿主
- 支持接口 - 丁尔男
- 匿名结构体
- wasm 后端的标识符生成规则
- 内置的 copy 函数
- 中文语法 - 赵普明
- p5
- 包：strconv、math、base58、container、unicode、testing、syscall、regexp、httputil、image、json、ioutil、rot13、base60、base64、bigint、ini、strings、bytes